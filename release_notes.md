# Release Notes

## v0.1.1

- スライドのスタイルをアップデート

## v0.1.0

### スライドモードを実装

marp ライクのスライドモードを実装

```
---
marp: true
---
```

から記述することで、自動的にスライドモードに移行。

## v0.0.28

- プレビューヘッダのスタイルを修正

## v0.0.27

- YouTubeの埋め込みエラーを修正
  - iframe の置換内容を YouTube のものと統一
- マークダウン入力支援ボタンを追加（H1、電話番号、メール）

## v0.0.26

可読性を向上させるため、Katex による数式の配置をセンターから左寄せに変更。

**index.html**

```html
.katex-display > .katex { text-align: left; margin-left: 5%; }
```

## v0.0.25

- 起動後にエディターにフォーカスする処理追加

## v0.0.24

### YouTubeの埋め込みに対応

- 独自のトークナイザを実装し、YouTubeのみを許可するiframeにパース
  - 一度、独自タグにパース後、最終的にiframeに変換
- 記述方法: `@[youtube](https://www.youtube.com/embed/jNQXAC9IVRw?si=Tv9xSOiNLmGs8i-g)`

### HTML出力

- PDF以外にもHTMLの出力機能を実装

### CSSの変更

- マークダウンに適用するCSSをユーザーが変更できるようにアップデート
  - 初回起動時にユーザーディレクトリに `.ageha` が作成される
  - JSON設定ファイルにCSSの場所が記述されており、自身で用意したCSSに変更、またはデフォルトで `~/.ageha` ディレクトリ内に作成される `ageha.css` により変更。

## v0.0.23

- コードブロックにコピーボタンを追加
  - プレビューのみ
  - 印刷では非表示となる

## v0.0.22

- 全画面表示時のエディターとプレビューの高さを調整

## v0.0.21

- ファイルパスの表示位置を画面左下からWindow titleに変更

## v0.0.20

- ファイルパスの描画位置を修正（エディタの高さを調整）

## v0.0.19

- 新規プロセスを起動できるようにアップデート
- ボタンUIを縮小し、エディタ部分を拡大

## v0.0.18

- エディタの設定情報を初期化ロードする場所をEditor.vueに変更
- Viewerウィンドウのサイズを変更

## v0.0.17

- ユーザーの設定j状態（プレビューの表示、マークダウン記号の入力ボタンの表示）保存をローカルストレージから [tauri-store-plugin](https://v2.tauri.app/ja/plugin/store/) に変更
- Viewerモードを廃止
  - 代わりに別ウィンドウをビューアとして開く機能を導入

## v0.0.16

- ファイル読み込みからマークダウンファイルを読み込んだ際にビューアモードで閲覧できない問題を修正
  - ファイル読み込み時にマークダウンデータストアを更新していなかったため

## v0.0.15

- エディターのtitle属性のコメントを修正
- ヘルプのインラインコードタグを修正

## v0.0.14

- ビューアモードを追加
  - アプリケーション終了時の状態で次回起動時のモード判定
  - 空ファイル又は通常起動の際はエディターモードで起動

## v0.0.13

- Rust側のコードを整理

## v0.0.12

- 初期画面サイズを縮小

## v0.0.11

- 改ページを行うカスタムトークン `@@@` を実装
  - 印刷時に改ページ可能とした。
- `py` `rs` `js` を除去（不要と判断）。

## v0.0.10

- Windowsインストーラを日本語化
- ウィンドウ初期サイズを拡大
- マークダウン記号入力支援ボタンの配置を変更
- `py` `rs` `js` を拡張子とするファイルも編集対象とした。

## v0.0.9

- Katexのスタイルを調整
- 数式の記述支援ボタンを追加

## v0.0.8

- Katexを導入し、数式の記述に対応。

## v0.0.7

- Mermaid記法による各種図形を使用可能とした。

## v0.0.6

- 画像ファイルを相対パスで読み込み可能とした。
- assetProtocol.scopeの設定不足によりMarkdown画像が読み込めない不具合を修正
  - Markdownファイル再読み込み後にローカル画像が表示されない問題（assetProtocol.scopeの許可範囲不足とパス解決処理の不備）

## v0.0.5

- アプリケーションライセンスを MIT に設定
- アプリタイトルをダブルクリックしてバージョン情報を表示するロジックを追加
- プレビューの表示非表示時のスタイルを微調整

## v0.0.4

- 未保存時に誤ってウィンドウクローズがなされた際の確認機能を実装

## v0.0.3

- READMEを更新
- CDN配信用のアプリアイコンを整理

## v0.0.2

- ファイルのドロップ機能を追加
  - 編集中（未保存）のファイルに対する保護機構を追加
- スタイルを調整

## v0.0.1

- 初期バージョン作成
- 基本的な機能実装の完了
- リリースノートの作成
